{
    "type": "module",
    "dependencies": {
        "express": "^4.18.2",
        "pg": "^8.8.0"
    },
    "scripts": {
        "pg:init": "pg_ctl init -D server",
        "pg:start": "pg_ctl -D server start",
        "pg:createdb": "createdb -h 127.0.0.1 app",
        "pg:stop": "pg_ctl -D server stop",
        "start": "NODE_ENV=development && node index.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
