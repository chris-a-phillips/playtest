// ? StartBattlePhase
    // ? SetEntPositions
    // ? InitiateEntActions
    // ? CheckEntStatus
    // ? CheckEntStatus
    // ? CheckChainStatus
    // ? EndBattlePhase

game.entSetup.orderEntsBySpeed(entsArray)


game.setPositions(entsArray)
game.inititiateActionSequence(entsArray)
